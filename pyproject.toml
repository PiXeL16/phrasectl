[project]
name = "phrasectl"
version = "0.1.0"
description = "System-wide AI text rephrasing tool for Linux/Wayland"
requires-python = ">=3.11"
dependencies = ["anthropic"]

[project.optional-dependencies]
dev = ["pytest", "pytest-mock"]

[tool.pytest.ini_options]
testpaths = ["tests"]
